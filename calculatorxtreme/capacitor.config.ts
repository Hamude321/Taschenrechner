import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.calculatorxtreme',
  appName: 'calculatorxtreme',
  webDir: 'www'
};

export default config;
 